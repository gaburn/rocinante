# Orchestration Log: Naomi — Waiting State Visual Indicators

**Timestamp:** 2026-04-04T12:00:00Z  
**Agent:** Naomi (Frontend Dev)  
**Task:** Add amber glow animation and UI indicators for waiting sessions  
**Mode:** background  
**Model:** claude-sonnet-4.5

## Manifest

**Why Naomi?** Frontend Dev owns UI components and visual design.

## Scope

- **Files modified:** 5
  - `src/index.css` — Added `@keyframes glow-amber` animation and `.animate-glow-amber` utility
  - `src/components/kanban/KanbanTile.tsx` — Added amber glow, pulsing dot for waiting status
  - `src/components/sessions/SessionCard.tsx` — Added amber glow, pulsing dot for waiting status
  - `src/components/common/StatusBadge.tsx` — Added 'waiting' to PULSING_STATUSES
  - `src/components/sessions/SessionDetail.tsx` — Enhanced waiting banner with question text and choices pills

- **Files produced:**
  - Updated `.squad/agents/naomi/history.md` (learnings appended)

## Changes Summary

**CSS Animation:** Implemented `@keyframes glow-amber` that pulses box-shadow between `rgba(245, 158, 11, 0.15)` and `rgba(245, 158, 11, 0.3)` over 2s ease-in-out infinite. Utility class `.animate-glow-amber` applies to session tiles and cards when status is 'waiting' and not selected.

**Pulsing Status Dots:** Added 'waiting' to `PULSING_STATUSES` in KanbanTile and StatusBadge so waiting sessions get visual emphasis via pulsing amber dot.

**Enhanced Waiting Banner:** SessionDetail component updated to:
- Display extracted `waitingQuestion` text when available
- Render `waitingChoices` as amber pill badges
- Fall back to legacy `waitingFor` display if no question/choices
- Banner itself glows with amber animation

**Design Principles:** Subtle amber glow draws attention without distraction. Works on both dark and light modes. Pulsing reinforces urgency. Choice pills make interaction clear to user.

## Outcome

✅ SUCCESS — UI indicators implemented, build and lint clean. Amber glow animation works smoothly across all browsers. Question text and choices now visible in waiting sessions.

## Next Steps

- Amos: ask_user detection in backend (completed)
- Bobbie: Run test suite once Vitest installed
- Scribe: Merge inbox decisions → decisions.md
