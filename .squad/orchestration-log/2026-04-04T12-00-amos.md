# Orchestration Log: Amos — ask_user Tool Detection

**Timestamp:** 2026-04-04T12:00:00Z  
**Agent:** Amos (Backend Dev)  
**Task:** Implement ask_user tool detection for waiting state  
**Mode:** background  
**Model:** claude-sonnet-4.5

## Manifest

**Why Amos?** Backend Dev owns data pipeline and status derivation logic.

## Scope

- **Files modified:** 3
  - `server/services/statusDeriver.ts` — Added `getAskUserRequest()` helper, updated detection logic
  - `server/services/sessionMapper.ts` — Pass `waitingQuestion`/`waitingChoices` through to Session object
  - `src/types/index.ts` — Added `waitingQuestion?: string` and `waitingChoices?: string[]` to Session and DerivedStatus interfaces

- **Files produced:**
  - `.squad/decisions/inbox/amos-ask-user-detection.md` (decision record)
  - Updated `.squad/agents/amos/history.md` (learnings appended)

## Changes Summary

**Type System:** Extended Session and DerivedStatus interfaces with `waitingQuestion` and `waitingChoices` fields to expose assistant's question and multiple-choice options to frontend.

**Detection Logic:** Implemented `getAskUserRequest()` that:
- Normalizes `ask_user` tool name variants (`askUser`, `ask-user`)
- Checks `name`, `toolName`, `tool_name` fields for robustness across Copilot versions
- Extracts question from `parameters.question` or `arguments.question`
- Extracts choices from `parameters.choices` or `arguments.choices`
- Returns `{ question, choices }` or `null`

**Status Classification:** ask_user tool requests now:
- Filtered out of active detection (not execution)
- Detected early in waiting detection step
- Set status to `'waiting'` with `waitingFor: 'user input'`
- Include extracted question/choices for frontend display

**Data Flow:** `statusDeriver` → `sessionMapper` → frontend via Session API

## Outcome

✅ SUCCESS — Implementation complete, types updated, build and lint clean. Tests written by Bobbie are awaiting Vitest installation and are expected to pass once installed.

## Next Steps

- Bobbie: Run test suite once Vitest installed (P0 item from audit)
- Scribe: Merge inbox decisions → decisions.md
- Frontend: Display question/choices in waiting sessions (Naomi completed amber glow UI)
